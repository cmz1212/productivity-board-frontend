import { Link } from "react-router-dom";

export default function CreateUser() {
  return (
    <div>
      Create user
      <br />
      <button>
        <Link to="/">Home</Link>
      </button>
    </div>
  );
}
